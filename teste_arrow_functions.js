// const hello = () => { console.log ("oi")}
// hello()

// const hello = () => console.log("Oi")
// hello()

// const soma = (a, b) => { return a + b }
// console.log(soma(2, 3))

// const soma = (a, b) => a + b
// console.log(soma(2, 3))

const contaComplicada = (a, b, c) => {
    console.log("oi")
    return a * a + b * 2 + c * a + a * a + a * b
}

console.log(contaComplicada(1, 2, 3))